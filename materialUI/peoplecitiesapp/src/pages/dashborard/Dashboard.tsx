import { BaseLayoutPages } from "../../shared/layouts"

export const Dashboard: React.FC= () => {
    return (
        <BaseLayoutPages title="Página Inicial">
            Conteúdo
        </BaseLayoutPages>
    )
}